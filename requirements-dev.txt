-r requirements.txt

# Test çalıştırma için (production'da gerekmez)
pytest>=8.0.0
httpx>=0.27.0

# E2E (gerçek tarayıcı) testleri için — tests/e2e/
# Kurulumdan sonra bir kere: python3 -m playwright install chromium
playwright>=1.45.0
